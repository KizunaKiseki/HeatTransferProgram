{
	"Numerical methods are approximations. What is the \"cost of being wrong\" if your grid size is too coarse in a high-stakes design like a nuclear reactor core? ": [
		"If the grid size is too coarse in for example HVAC design, the temperature gradients in a room could not be accurate leading to hot and cold spots. This could lead to undersized equipment, higher energy use, increased humidity levels causing condensation and mold to build up over time. In operating rooms, for example, this could lead to infection or contamination which could potentially harm patients."
	],
	"Computation is expensive. How do you decide when a \"good enough\" coarse-mesh solution creates more value than a perfectly converged, time-consuming model? ": [
		"A coarse-mesh is good enough when it captures the trends like peak temperatures without changing as the value increases or decreases. The goal of when deciding when a solution is good enough is to adjust the mesh to see how sensitive it is to different results. If the results have not changed significantly then the errors that come with computation methods like discretiztion and round-off errors are insignificant and the solution is good enough. Finally, the overall decision for the results should come from standards and codes."
	],
	"You\u2019ve seen how derivatives become differences. Identify a \"real-world\" boundary condition (like a moving car's radiator) where the standard math feels inadequate. How would you iterate your code to adapt? ": [
		"An example like a car's radiator convection coefficent changes as the vehicle is accelerating, and stoped. Therefore, a constant boundary condition is not adequate since it does not reflect the realistic conditions the car's radiator is experiencing. In order to adjust the code to adapt to these changes, I would make it dependent on time and velocity."
	],
	"Analytical solutions are limited to simple shapes. What \"unconventional\" or complex geometry in a modern product (e.g., a custom heat sink) represents an opportunity for innovation that only numerical methods can solve? ": [
		"An example of a unconventional product is snow melting systems in HVAC systems. Snow melting systems are multidimensional conduction systems. Numerical methods can help determine where to lay tubing to avoid cold spots preventing snow from melting."
	],
	"Software packages like ANSYS use these same methods. Why is it essential to understand the \"math under the hood\" before you trust the output of a professional simulation tool? ": [
		"It is important to understand the math for software packages like ANSYS or TRACE 3D since the models could be wrong and need engineering verifications to know if the results are resonable. If you did not understand the math behind the system then this could lead to trusting a inaccurate solution causing major issues. An example, is TRACE 3D often does not accurately estimate the occupancy of a room correctly. The engineer must check the results to see if they are phyically reasonable instead of trusting a software which can cause safety concerns if the results were blindly trusted."
	]
}